# This is an autogenerated file - generation date {{ date }}
@prefix : <#> .
@prefix pokemon: <https://nick.datapod.igrant.io/schemas/POKEMON/TRADING_CARD_GAME.ttl> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .

:{{ setId }} a pokemon:PokemonSet ;
    rdfs:label "{{ label }}" ;
    rdfs:comment "{{ comment }}" ;
    pokemon:setId "{{ setId }}" ;
    pokemon:name "{{ name }}" ;
    pokemon:series "{{ series }}" ;
    pokemon:printedTotal "{{ printedTotal }}" ;
    pokemon:total "{{ total }}" ;
    pokemon:releaseDate "{{ releaseDate }}" ;
    pokemon:updatedAt "{{ updatedAt }}" ;
    pokemon:imagesLogo "{{ images.logo }}" ;
    pokemon:imagesSymbol "{{ images.symbol }}" .